//
//  NeuLedgerApp.swift
//  NeuLedger
//
//  ========================================================================================
//  ⚠️ 應用程式進入點 (@main) 已經搬移至 `Features` 模組！
//  ========================================================================================
//
//  為了方便統一管理 Navigation (導覽)、TCA (The Composable Architecture) 狀態
//  以及各種核心依賴 (實體化 Dependency 等)，我們將 `NeuLedgerApp` 的實作
//  與 `@main` 標記移到了 `Features` 模組中的 `RootView.swift`。
//
//  保留此檔案（沒有實質程式碼）的目的：
//  1. 維持 Xcode App Target 的基本預設檔案結構，以便未來查找。
//  2. 提醒後來的開發者或自己，專案的起點已經轉移到 Feature 模組化架構內。
//
//  要尋找 App 進入點，請前往：
//  👉 `Features/Sources/Features/RootView.swift`
//
